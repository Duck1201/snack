# @snack-ai/cli

Technical foundation preview for SNACK. This version provides the `snack` executable, validated
local configuration, private SQLite lifecycle, and structural diagnostics. It does not read AI
client data or produce a forecast.

Requires Node.js 24.
