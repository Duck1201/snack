node_modules/
*.tgz
coverage/
.nyc_output/
*.log
graphify-out/

# Local agent and editor state: per-machine, and sometimes per-person.
.claude/settings.local.json
.idea/
.vscode/
*.swp
*~

# Secrets never belong in a repository whose premise is that nothing leaves your machine.
.env
.env.*

# What a local run can leave behind. SNACK writes to XDG paths rather than the working tree, but a
# hand-seeded database or an export lands wherever the command was pointed.
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal
*.ndjson

# Operating-system leftovers.
.DS_Store
Thumbs.db
desktop.ini

# Reference images dropped in while designing. Not project assets.
terminal-design.jpg
